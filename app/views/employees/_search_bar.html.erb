<div class="row">
  <div class="d-flex justify-content-center mb-4">
    <%= form_with url: search_employees_path, method: :get, class: "d-flex" do |form| %>
      <%= form.text_field :query , placeholder: "Enter name of employees", class: "form-control me-2" %>
      <%= form.submit "Search", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
