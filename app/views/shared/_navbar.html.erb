<% if user_signed_in? %>
  <div class="navbar navbar-expand-lg">
    <div class="container-fluid">
      <div>
        <%= link_to "Products", products_path, class: "col-2 navbar-brand" %>
        <%= link_to "Orders", orders_path, class: "col-2 navbar-brand" %>
        <%= link_to "Employees", employees_path, class: "col-2 navbar-brand" %>
      </div>
      <div>
        <p class="my-auto">You are <%= current_user.role.capitalize %></p>  
      </div>
      <div class="d-flex justify-content-center align-items-center">
        <div class="me-3"> Welcome, <%= current_user.username %> </div>
        <%= link_to "Sign out", destroy_user_session_path, data: { turbo_method: :delete } , class: "btn btn-dark" %> 
      </div>
    </div>
  </div>
<% end %>
